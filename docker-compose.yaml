services:
  redis:
    image: redis:8.0.1-alpine
    restart: always
    ports:
      - 0.0.0.0:6379:6379
    command: redis-server --save 20 1 --loglevel warning --requirepass rhost21
    volumes:
      - ./redis:/data
volumes:
  redis:
    driver: local